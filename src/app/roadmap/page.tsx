import { ArrowRight, Check, Circle, LockKeyhole } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/feedback";
const nodes=[{state:"done",title:"Python foundations",detail:"CLI data project reviewed"},{state:"done",title:"Applied statistics",detail:"Evaluation metrics explained"},{state:"next",title:"Machine-learning workflow",detail:"Train and document a baseline model"},{state:"later",title:"LLM application safety",detail:"Grounding and injection tests"},{state:"later",title:"Deploy evidence",detail:"Monitored API and project write-up"}];
export default function RoadmapPage(){return <main id="main-content" className="roadmapPage"><header><div><p className="eyebrow">AI Engineer · roadmap v1</p><h1>One useful move at a time.</h1><p className="lede">Your profile suggests what may be complete. You confirm every milestone.</p></div><Badge tone="success">2 of 5 confirmed</Badge></header><section className="roadmapGraph" aria-label="AI Engineer roadmap">{nodes.map((node,index)=><article key={node.title} className={node.state}><span className="nodeIcon">{node.state==="done"?<Check/>:node.state==="next"?<Circle/>:<LockKeyhole/>}</span><div><p className="pathLabel">{node.state==="done"?"Confirmed":node.state==="next"?"Next best move":"Later"}</p><h2>{node.title}</h2><p>{node.detail}</p>{node.state==="next"&&<Button>Open milestone<ArrowRight size={17}/></Button>}</div>{index<nodes.length-1&&<span className="connector" aria-hidden="true"/>}</article>)}</section></main>}
