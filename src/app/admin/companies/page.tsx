import { AdminSectionPlaceholder } from "@/features/admin/admin-section-placeholder";

export default function CompaniesPage() {
  return <AdminSectionPlaceholder title="Companies" description="Manage verified employer records before they are attached to placement drives." />;
}
