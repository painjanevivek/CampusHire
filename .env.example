NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
INTERNAL_API_URL=http://localhost:8000/api/v1
# Server-only display flag. The backend must separately enable and seed demo accounts.
DEMO_LOGIN_ENABLED=false
# Production-like deployments use the gateway's same-origin HTTPS API path,
# for example https://staging.example.edu/api/v1.
